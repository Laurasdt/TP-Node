extends layout

block title
  | Users List  

block content
  h1 List of Users
  table(class="box")
    tr
      th Name
      th Date of Birth
    each student in students
      tr
        td= student.name
        td= student.birth
